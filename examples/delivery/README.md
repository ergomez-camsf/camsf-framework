# Delivery Example

This example models a context-aware mobile application for logistics and package tracking.

The model uses GPS and IoT-based context to update delivery tracking and send contextual notifications when a package is delayed.

Artifacts:
- delivery.dsl: CAMS-F textual model.
- delivery.png: graphical representation of the example model.
